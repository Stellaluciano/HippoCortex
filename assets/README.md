# Assets

Binary assets are not committed in this environment.

To generate the architecture PNG locally, run:

```bash
python scripts/render_diagram.py
```

This will create `assets/hippocortex_architecture.png` on your machine.
