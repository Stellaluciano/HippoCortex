# HippoCortex TypeScript SDK (Placeholder)

Python SDK is the production path for MVP.

Planned TS surface:
- `memory.hippo.addEvent(...)`
- `memory.cortex.search(...)`
- `memory.consolidate(...)`
- `memory.buildContext(...)`
